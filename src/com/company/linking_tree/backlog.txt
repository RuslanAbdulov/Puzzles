Does LevelMap metadata needed?
accept Entity::method reference instead of key
method checking if there is a key without value
method populating nodes without values using pair(key, value) or collection of values and keyExtractor method
method linking Nodes/keys of one level with child keys/values
public void find(Object... args)
implement add child with/out value first, add parent later
sanity check - every node should be reachable from one of the 1st level nodes
build LinkingTree using reflection, traverse fields/methods from root to child classes

//TODO check if with Class<?>... args all levels can be defined with child types right away
public LinkingTree(Class<?>... args) {
    for (int i = 0; i < args.length; i += 2) {
        if (args.length - i > 3) {
            addLevel(args[i], args[i + 1], args[i + 2], args[i + 3]);
        } else if (args.length - i > 1) {
            addLevel(args[i], args[i + 1]);
        } else {
            throw new IllegalArgumentException("Number of arguments must be even");
        }
    }
}